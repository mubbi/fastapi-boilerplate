# Empty in the boilerplate — domain models are added per project.
# Importing this module must not register any models so that Alembic's
# autogenerate produces an empty diff against an empty DB by default.
